# RA Tweaks — Instructions for Claude Code

## Vault linkage
This repo's knowledge base lives in the vault, not in this repo:
- Vault notes: `~/vault/ra-tweaks/`
- Architecture decisions: `~/vault/ra-tweaks/architecture/decisions.md`
- Session logs: `~/vault/ra-tweaks/logs/`
- Graphify knowledge graph: `~/vault/ra-tweaks/graph/` (`GRAPH_REPORT.md`, `graph.json`, per-entity notes)

See `~/vault/CLAUDE.md` for the full zettelkasten rules and the `/vault-save` / `/vault-resume` skills — they apply here using the vault paths above.

## Never do
- Don't create notes or logs inside this repo — they belong in the vault path above
- Don't move this repo into the vault
